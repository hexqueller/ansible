output {
        elasticsearch {
            hosts    => "http://localhost:9200"
            index    => "websrv-%{+YYYY.MM}"
	    user => "elastic"
	    password => "{{ elastic_password }}"
        }
}