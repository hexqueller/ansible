output {
        elasticsearch {
            hosts    => "http://localhost:9200"
            index    => "logs-%{+YYYY.MM}"
	    user => "elastic"
	    password => "{{ elastic_password }}"
        }
}